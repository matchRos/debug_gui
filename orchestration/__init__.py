"""
Action/orchestrator layer for the debug GUI pipeline.

This is intentionally ROS-agnostic for now so we can introduce a clean
execution model before binding the same actions to ROS action servers.
"""

